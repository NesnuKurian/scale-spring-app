apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "scale-spring-app.fullname" . }}
  labels:
    app: {{ include "scale-spring-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "scale-spring-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "scale-spring-app.name" . }}
    spec:
      containers:
        - name: {{ include "scale-spring-app.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.targetPort }}
